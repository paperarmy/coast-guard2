# ── Python ──────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# ── 환경변수 (API 키 포함) ────────────────────────────────────
.env
.env.*
!.env.example

# ── 원본 수집 데이터 (군사 위치 정보 포함 가능) ──────────────
/data/
# backend/data/processed/ 는 Vercel 서버리스 함수 접근용으로 git 포함
# backend/data/assets.json 도 기본값으로 git 포함

# ── Vercel ──────────────────────────────────────────────────
.vercel/

# ── IDE / OS ────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
